name: Lint and Test (Disabled)

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Lint checks are currently disabled."
